stages:
  - test

pytest:
  stage: test
  image: ghcr.io/astral-sh/uv:python3.12-alpine
  script:
    - uv sync
    - pytest --junitxml=report.xml
  artifacts:
    when: always
    reports:
      junit: report.xml